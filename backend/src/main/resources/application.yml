# spring:
#   datasource:
#     driver-class-name: com.mysql.cj.jdbc.Driver
#     url: ${DB_URL:jdbc:mysql://localhost:3306/lms?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC}
#     username: ${DB_USERNAME:root}
#     password: ${DB_PASSWORD:Kir#n9833}

#   jpa:
#     hibernate:
#       ddl-auto: update
#     show-sql: true
#     properties:
#       hibernate:
#         format_sql: true

#   mvc:
#     throw-exception-if-no-handler-found: true

#   web:
#     resources:
#       add-mappings: false


# # ⭐ MUST BE OUTSIDE SPRING BLOCK ⭐
# app:
#   jwtSecret: ${JWT_SECRET:mySecretKey1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ}
#   jwtExpirationMs: ${JWT_EXPIRATION:86400000}

# default-admin:
#   username: ${ADMIN_USERNAME:admin}
#   password: ${ADMIN_PASSWORD:admin123}
#   email: ${ADMIN_EMAIL:admin@gmail.com}

# logging:
#   level:
#     com.lms.dev: ${APP_LOG_LEVEL:DEBUG}
#     org.springframework.security: ${SECURITY_LOG_LEVEL:DEBUG}
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  mvc:
    throw-exception-if-no-handler-found: true

  web:
    resources:
      add-mappings: false

# -----------------------------
# APP CONFIG (OUTSIDE SPRING)
# -----------------------------
app:
  jwtSecret: ${JWT_SECRET}
  jwtExpirationMs: ${JWT_EXPIRATION:86400000}

  default-admin:
    username: ${ADMIN_USERNAME}
    password: ${ADMIN_PASSWORD}
    email: ${ADMIN_EMAIL}

logging:
  level:
    com.lms.dev: ${APP_LOG_LEVEL:DEBUG}
    org.springframework.security: ${SECURITY_LOG_LEVEL:DEBUG}
